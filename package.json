{
  "name": "job-search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Radar de vagas de estágio/jr em desenvolvimento de software (Node/TypeScript)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"tests/**/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
